{
  "name": "team-task-manager",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build --workspace client && npm run prisma:generate --workspace server",
    "start": "npm run prisma:migrate:deploy --workspace server && npm start --workspace server"
  }
}
